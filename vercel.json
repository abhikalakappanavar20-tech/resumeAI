{
    "version": 2,
    "installCommand": "pip install -r requirements-vercel.txt",
    "builds": [
        {
            "src": "vercel_app/index.py",
            "use": "@vercel/python",
            "config": {
                "maxDuration": 30
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "vercel_app/index.py"
        }
    ]
}
