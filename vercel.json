{
    "version": 2,
    "installCommand": "pip install --no-compile --no-cache-dir -r requirements-vercel.txt",
    "buildCommand": "",
    "outputDirectory": "staticfiles",
    "builds": [
        {
            "src": "vercel_app/index.py",
            "use": "@vercel/python",
            "config": {
                "maxDuration": 30
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "vercel_app/index.py"
        }
    ]
}
